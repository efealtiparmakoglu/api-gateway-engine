apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-gateway-engine-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: api-gateway-engine.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: api-gateway-engine-service
            port:
              number: 80
